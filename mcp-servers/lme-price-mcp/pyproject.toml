[project]
name = "lme-price-mcp"
version = "0.1.0"
description = "MCP Server — LME 矿产价格行情"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"
